apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: rover-zone-audit
  namespace: {{ .Values.namespace }}
spec:
  broker: {{ .Values.broker.name }}
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: rover-zone-audit
---
apiVersion: eventing.knative.dev/v1
kind: Trigger
metadata:
  name: rover-zone-approval
  namespace: {{ .Values.namespace }}
spec:
  filter:
    attributes:
      type: dev.knative.apiserver.resource.add
  broker: {{ .Values.broker.name }}
  subscriber:
    ref:
      apiVersion: serving.knative.dev/v1
      kind: Service
      name: rover-zone-approval